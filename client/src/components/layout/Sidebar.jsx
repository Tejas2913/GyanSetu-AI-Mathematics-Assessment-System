// Sidebar — Desktop sidebar navigation (optional, can be enabled for teacher/parent dashboards)
// Currently minimal — expand in Phase 6

export default function Sidebar() {
  return null // Placeholder — implement if teacher/parent dashboard needs a sidebar
}
